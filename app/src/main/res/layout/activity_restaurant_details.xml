<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/backgroundColor"
    android:gravity="center_horizontal">

    <!-- com.example.personalrestaurantguide.Restaurant Map -->
    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginBottom="16dp" />

    <!-- com.example.personalrestaurantguide.Restaurant Name -->
    <EditText
        android:id="@+id/restaurant_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Restaurant Name"
        android:background="@drawable/edit_text_background"
        android:textColor="@color/primaryTextColor"
        android:padding="12dp"
        android:layout_marginBottom="8dp" />

    <!-- com.example.personalrestaurantguide.Restaurant Address -->
    <EditText
        android:id="@+id/restaurant_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Address"
        android:background="@drawable/edit_text_background"
        android:textColor="@color/primaryTextColor"
        android:padding="12dp"
        android:layout_marginBottom="8dp" />

    <!-- com.example.personalrestaurantguide.Restaurant Phone -->
    <EditText
        android:id="@+id/restaurant_phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Phone Number"
        android:background="@drawable/edit_text_background"
        android:textColor="@color/primaryTextColor"
        android:padding="12dp"
        android:layout_marginBottom="8dp" />

    <!-- com.example.personalrestaurantguide.Restaurant Description -->
    <EditText
        android:id="@+id/restaurant_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Description"
        android:background="@drawable/edit_text_background"
        android:textColor="@color/primaryTextColor"
        android:padding="12dp"
        android:layout_marginBottom="8dp" />

    <!-- com.example.personalrestaurantguide.Restaurant Tags -->
    <EditText
        android:id="@+id/restaurant_tags"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Tags (e.g., Vegan, Italian)"
        android:background="@drawable/edit_text_background"
        android:textColor="@color/primaryTextColor"
        android:padding="12dp"
        android:layout_marginBottom="16dp" />

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/get_directions_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Get Directions"
            android:background="@color/buttonColor"
            android:textColor="@color/primaryTextColor"
            android:layout_marginEnd="8dp"
            android:padding="12dp" />

        <Button
            android:id="@+id/save_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Save"
            android:background="@color/buttonColor"
            android:textColor="@color/primaryTextColor"
            android:padding="12dp" />
    </LinearLayout>

</LinearLayout>
